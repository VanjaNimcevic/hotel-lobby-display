<?xml version="1.0" encoding="utf-8"?>
<!--
    Fullscreen signage player surface.

    Hosts four content renderers stacked on top of each other. Only one is
    ever VISIBLE at a time; PlaybackController toggles visibility as the
    playlist advances. Background is solid black so letterboxed video and
    images look right on a lobby TV.
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/player_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:keepScreenOn="true"
    tools:context=".MainActivity"
    tools:deviceIds="tv">

    <!-- VIDEO: Media3 player surface. use_controller=false hides all playback
         controls (this is signage, not a media player). -->
    <androidx.media3.ui.PlayerView
        android:id="@+id/video_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/black"
        android:visibility="gone"
        app:resize_mode="fit"
        app:use_controller="false" />

    <!-- IMAGE: fitCenter keeps signage artwork uncropped on any panel ratio. -->
    <ImageView
        android:id="@+id/image_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/black"
        android:contentDescription="@string/image_content_description"
        android:scaleType="fitCenter"
        android:visibility="gone" />

    <!-- WEB: a WebView is created and added here programmatically (APV-18)
         so it can be destroyed cleanly and never leaks the Activity. -->
    <FrameLayout
        android:id="@+id/web_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/black"
        android:visibility="gone" />

    <!-- TEXT / BANNER: layout_gravity is switched at runtime to place the
         banner at top, center or bottom (APV-17). -->
    <TextView
        android:id="@+id/text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@android:color/black"
        android:gravity="center"
        android:lineSpacingMultiplier="1.2"
        android:padding="64dp"
        android:textColor="@android:color/white"
        android:textSize="48sp"
        android:visibility="gone"
        tools:text="Welcome to the Grand Hotel"
        tools:visibility="visible" />

    <!-- DEBUG overlay (APV-27): tiny status readout in the corner. Hidden unless
         DebugOverlay.ENABLED is true; never shown in production-like playback. -->
    <TextView
        android:id="@+id/debug_overlay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="16dp"
        android:background="#B0000000"
        android:fontFamily="monospace"
        android:padding="8dp"
        android:textColor="#FF00FF66"
        android:textSize="14sp"
        android:visibility="gone"
        tools:ignore="HardcodedText" />

</FrameLayout>
