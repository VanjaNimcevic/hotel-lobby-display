<?xml version="1.0" encoding="utf-8"?>
<!-- Fullscreen player surface. Four renderers stacked; PlaybackController shows one at a time. -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/player_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    android:keepScreenOn="true"
    tools:context=".MainActivity"
    tools:deviceIds="tv">

    <!-- VIDEO -->
    <androidx.media3.ui.PlayerView
        android:id="@+id/video_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/black"
        android:visibility="gone"
        app:resize_mode="fit"
        app:use_controller="false" />

    <!-- IMAGE -->
    <ImageView
        android:id="@+id/image_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/black"
        android:contentDescription="@string/image_content_description"
        android:scaleType="fitCenter"
        android:visibility="gone" />

    <!-- WEB: WebRenderer adds/destroys a WebView here (kept out of XML to avoid leaks). -->
    <FrameLayout
        android:id="@+id/web_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@android:color/black"
        android:visibility="gone" />

    <!-- TEXT / BANNER: layout_gravity is set at runtime (top / center / bottom). -->
    <TextView
        android:id="@+id/text_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:background="@android:color/black"
        android:gravity="center"
        android:lineSpacingMultiplier="1.2"
        android:padding="64dp"
        android:textColor="@android:color/white"
        android:textSize="48sp"
        android:visibility="gone"
        tools:text="Welcome to the Grand Hotel"
        tools:visibility="visible" />

    <!-- DEBUG overlay: hidden unless DebugOverlay.ENABLED is true. -->
    <TextView
        android:id="@+id/debug_overlay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_margin="16dp"
        android:background="#B0000000"
        android:fontFamily="monospace"
        android:padding="8dp"
        android:textColor="#FF00FF66"
        android:textSize="14sp"
        android:visibility="gone"
        tools:ignore="HardcodedText" />

</FrameLayout>
